go into docker terminal
docker exec -it shop bash

sudo chmod -R 777 custom/plugins/ThStarterTheme/src/Resources/app/storefront/dist
sudo chmod -R 777 custom/plugins/ThStarterPlugin/src/Resources/app/storefront/dist
bin/watch-storefront.sh

adminer
http://localhost/adminer.php?server=127.0.0.1&username=root

bin/build-storefront.sh
    -to build the storefront (usuall you just need to do this) 

bin/build-administration.sh
    -to build the administration 

bin/console theme:compile
    -to compile the theme 

bin/console assets:install 